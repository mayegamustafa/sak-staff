# URL of the SAK backend API server
# Change to the production server IP/hostname when deploying
SAK_SERVER_URL=http://localhost:4000

# Vite env var (used by browser shim when running in-browser at localhost:5173)
VITE_SERVER_URL=http://localhost:4000
